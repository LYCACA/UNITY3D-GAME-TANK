using UnityEngine;
using System.Collections;

public class HPEnemy : MonoBehaviour{

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
    //    transform.localScale = new Vector3( / 4, 0.01f, 0.3f);
    //    transform.position = new Vector3(transform.position.x - hp / 8, transform.position.y,transform.position.z);
	}
}
